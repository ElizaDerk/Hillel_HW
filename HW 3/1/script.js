let startX = +prompt("Let's enter the number X");
let endY = +prompt("Let's enter the number Y");
    if(startX > 0 && endY > 0) {
        for (startX = 0 ; startX <= endY; startX++ ) {
            alert(startX);
        }
    }else{
        alert("Try again!")
    }

