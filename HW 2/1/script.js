let userName = prompt('Enter you name!');
let userPassword = prompt('Enter your password')
    if (userName === 'admin' && userPassword === 'qWeRty123'){
        alert('Entrance allowed!')
    }
    else {
        alert('No entry!')
    }
